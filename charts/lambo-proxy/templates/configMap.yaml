---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
  labels:
    {{- include "lambo-proxy.labels" . | nindent 4 }}
data:
  config.yaml: |
{{.Values.config |indent 4}}

